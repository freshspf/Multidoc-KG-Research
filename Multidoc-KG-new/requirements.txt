# Core dependencies for Python 3.11.14
# Data validation and modeling
pydantic>=2.0.0,<3.0.0

# Environment variable management
python-dotenv>=1.0.0,<2.0.0

# HTTP requests (used for custom API calls)
requests>=2.31.0,<3.0.0

# LLM API client (required for standard OpenAI API calls)
# Note: Code uses self.client.chat.completions.create() which requires openai library
openai>=1.0.0,<2.0.0

# Vector store and semantic search
sentence-transformers>=2.2.0,<3.0.0
faiss-cpu>=1.7.4,<2.0.0

# Numerical computing (dependency for faiss and sentence-transformers)
numpy>=1.24.0,<2.0.0

# Graph database
neo4j>=5.0.0,<6.0.0